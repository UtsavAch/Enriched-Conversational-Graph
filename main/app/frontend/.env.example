# Only needed if the API runs somewhere other than http://127.0.0.1:8000
# during `npm run dev`. In production the app is served by FastAPI itself,
# so no configuration is required.
VITE_API_TARGET=http://127.0.0.1:8000
